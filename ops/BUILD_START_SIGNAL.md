# BUILD START SIGNAL

The KODEX vertical-slice implementation may begin when:

- the agent has read the canonical bootstrap;
- it is working from `wenu-frontend@feature/kodex-depth-engine`;
- it uses a feature branch;
- it is assigned KOD-19, KOD-20 or KOD-21;
- it reports an evidence-based preflight;
- deployment remains false.

The implementation does not need to wait for complete ingestion of every historical asset.
