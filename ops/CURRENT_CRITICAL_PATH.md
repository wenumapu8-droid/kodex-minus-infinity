# CURRENT CRITICAL PATH

Status: `CURRENT / RELEASE CONVERGENCE`

This file is an operational pointer, not canon and not a substitute for live repository/worktree inspection.

## Authority gate — always first

```text
PUBLIC RUNTIME
→ MINI PRODUCTION WORKTREES + LOCAL DIFFS
→ CURRENT REMOTE PRODUCT LINEAGE
→ EXACT-SHA CI / BROWSER EVIDENCE
→ GOVERNED DRIVE REFERENCE + 08E WIREFRAME
→ HISTORICAL PRs AS BOUNDED DONORS ONLY
```

Before any product-facing write, reconcile the authorized Mini production worktrees and preserve valid local work. A remote branch, stale queue, historical PR, or documentation snapshot must never overwrite newer valid Mini work.

## Current remote anchor

- implementation repo: `wenumapu8-droid/wenu-frontend`
- product host: PR `#101`
- branch: `feat/kodex-manifestation-recipe-p0-4`
- remote head observed during this reconciliation: `025247a93409444a793a0ff763923e639bc48cd7`
- exact-head evidence: `KODEX Core Runtime #479 / run 33201000700 = SUCCESS`
- truth state: `OPEN / DRAFT / NOT MERGED / NOT DEPLOYED / NOT CANON`

This remote anchor is evidence, not permission to discard Mini-local truth. Refresh it before every write.

## KODEX v1 critical path

```text
G0  RECONCILE MINI LOCAL TRUTH
    preserve valid worktrees / diffs / exact heads
    ↓
G1  LOCK ONE PRODUCT LINEAGE
    no parallel renderer/router/state/memory/design-system rebuild
    ↓
G2  REFERENCE-FIRST SCENE CONVERGENCE
    REFERENCE → WIREFRAME → DESKTOP → MOBILE → TEMPORAL/INTERACTION → REDUCED
    classify each scene KEEP / REFINE_TO_REFERENCE / MISSING
    ↓
G3  WHOLE-CORRIDOR EVIDENCE
    THRESHOLD → PORTAL/CROSSING → PROLOGUE → DESCENT → ARCHIVE → MACHINE → COSMOLOGY → RETURN
    desktop + 390×844 + 412×915 + reduced-motion + traversal/memory checks
    ↓
G4  CREATOR ACCEPTANCE
    technical/browser PASS does not imply creator acceptance
    ↓
G5  RELEASE CANDIDATE FREEZE
    exact SHA; only release-blocking changes allowed
    ↓
G6  BOUNDED RELEASE INTEGRATION
    integrate only proven deltas; no wholesale recovery/history merge
    ↓
G7  AUTHORIZED MINI DEPLOY + SMOKE
    production deploy from the authorized Mini/Cloudflare environment
    record deployed SHA, URL, timestamp and smoke evidence
    ↓
G8  OBSERVE → LEARN → GUARDRAIL
    convert recurring defects into tests, detectors, recipes or agent rules
```

## Product-first constraint

Any task not required by G0–G8 is `POST_V1` unless it removes a demonstrated blocker on the critical path.

Do not pre-empt this sequence with new foundation documents, ontology, renderer authority, router/state/memory authority, or broad research expansion.

## Truth boundary

`IMPLEMENTED ≠ CI_PASS ≠ BROWSER_VERIFIED ≠ CURATOR_REVIEWED ≠ CREATOR_ACCEPTED ≠ CANON ≠ MERGED ≠ DEPLOYED ≠ OBSERVED`
