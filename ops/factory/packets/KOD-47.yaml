schema_version: 1
work_packet:
  id: "KDX-WP-SCENE-ASSEMBLY-01"
  linear_issue: "KOD-47"
  title: "Visible Assembly 01 — end-to-end internal KODEX journey"
  lane: "SCENE_INTEGRATION"
  modality: "PRODUCT_ASSEMBLY"
  complexity: "C3"
  owner_profile: "LONG_CONTEXT_IMPLEMENTER"
  reviewer_profile: "ORCHESTRATOR"
  objective: "Turn the verified KODEX runtime and visual subsystems into one openable, end-to-end internal journey instead of adding more factory infrastructure."
  repository: "wenumapu8-droid/wenu-frontend"
  base_branch: "feature/kodex-vertical-slice-v0"
  dependencies:
    - "KOD-41"
    - "KOD-42"
  read_only_context:
    - "kodex-minus-infinity:canon/KODEX_CANON.md"
    - "kodex-minus-infinity:data/alphabet-topology.json"
    - "kodex-minus-infinity:data/experience-graph.json"
    - "kodex-minus-infinity:data/bridges/bridge-1-v0/**"
    - "wenu-frontend:src/lib/kodex/runtime/journey-state.ts"
    - "wenu-frontend:src/lib/kodex/runtime/journeyGraph/**"
    - "wenu-frontend:src/lib/kodex/runtime/edge-resolver.ts"
    - "wenu-frontend:PR #3 interaction runtime"
    - "wenu-frontend:PR #4 organism engine"
  may_edit:
    - "one new noindex internal journey route under src/pages/kodex/lab/**"
    - "directly required scene-integration component(s) under src/components/kodex/**"
    - "directly required journey integration runtime glue under src/lib/kodex/**"
    - "targeted tests/CI assertions for this internal route"
  must_not_edit:
    - "public production /kodex/ route"
    - "canonical A/M/Y assignments"
    - "unresolved B-L or N-X coordinate meanings"
    - "factory/control-plane architecture"
    - "secrets or deployment configuration"
  required_behavior:
    - "one persistent JourneyState across the full route"
    - "meaningful OBSERVE/REMEMBER divergence with different semantic memory"
    - "Archive interaction exposes at least one real source/provenance relation"
    - "HEART / 0 remains optional and restores exact route anchor/focus on exit"
    - "RETURN / +infinity is derived from the actual event trace"
    - "Back/Forward does not duplicate semantic writes"
    - "pointer/touch/keyboard semantic parity"
    - "full/reduced/off motion behavior"
    - "no-WebGL path remains semantically usable"
    - "no fabricated numeric telemetry"
  visual_rule:
    - "reuse validated renderers/effects before building new ones"
    - "when a final renderer is absent, use the simplest honest fallback preserving semantic interaction"
    - "never present a concept plate or Visual Passport as an implemented organism"
  acceptance:
    - "one noindex internal URL opens and completes end-to-end"
    - "Astro build passes"
    - "JourneyState + canonical graph + EdgeResolver tests pass"
    - "desktop, 390x844 and 412x915 browser evidence exists"
    - "keyboard completion is demonstrated"
    - "touch-equivalent semantics are exercised"
    - "reduced-motion and no-WebGL paths remain usable"
    - "one route with HEART and one route without HEART reach RETURN"
    - "same event trace yields the same Return signature"
    - "no public route replacement and no deploy"
  stop_conditions:
    - "KOD-41 has not approved the corrected JourneyState kernel"
    - "KOD-42 identifies unresolved branch/file-ownership conflict"
    - "implementation would invent an unresolved coordinate assignment"
    - "rights/provenance would require copying an unapproved reference"
    - "integration would require destructive branch history changes"
  status: "BLOCKED_DEPENDENCY"
  handoff_to: "ORCHESTRATOR"
  deployment: "BLOCKED"
