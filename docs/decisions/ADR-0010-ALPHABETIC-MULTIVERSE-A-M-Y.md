# ADR-0010 — ALPHABETIC MULTIVERSE TOPOLOGY A–M–Y

Status: `DEPRECATED / SUPERSEDED FOR V1 IMPLEMENTATION`  
Original date: `2026-08-06`  
Superseded by: `ADR-0011-SEVEN-SCENE-CORRIDOR-V1.md`

## Current authority notice

This architecture was an approved KODEX direction in August 2026, but it is **not the active V1 implementation topology**.

The current release architecture is:

```text
00 THRESHOLD
→ 01 PROLOGUE
→ 02 DESCENT
→ 03 ARCHIVE
→ 04 MACHINE
→ 05 COSMOLOGY
→ 06 RETURN
```

Agents must not use A–Y to assign current scenes, reopen the release architecture, create a parallel router or override the current Scene Bible / Hi-Fi targets unless a later explicit Ocín decision promotes A–Y again.

## Historical record

The full original ADR is preserved for provenance at:

`docs/decisions/historical/ADR-0010-ALPHABETIC-MULTIVERSE-A-M-Y.md`

Related historical registry:

`data/alphabet-topology.json`

These materials remain useful for research, recovery context and future exploration, but their implementation authority is `false` for V1.

## Supersession rationale

KODEX entered production-convergence around the seven principal scenes, with current design targets, web implementation, editorial structure, QA matrices and production controls aligned to that corridor.

Keeping the older A–Y document marked as current created a material risk that AI agents would rebuild deprecated architecture or treat historical coordinates as current canon.

The supersession is non-destructive: history remains inspectable, but authority is explicit.

## Deployment boundary

This deprecation does not authorize merge or deployment.

```text
DEPLOYMENT STATUS: BLOCKED
REQUIRED AUTHORIZATION: APROBAR DEPLOY
```
