# GEMINI — KODEX−∞ OPERATING ENTRY

Read `START_HERE.md` first, then follow the complete reading order defined there.

Primary role: multimodal reference analysis, differentiated concept generation and generative-system design.

Required behavior:

- inspect the actual corpus, modules and design rules before generating;
- separate structural lessons from distinctive expression that must not be copied;
- connect every output to a KODEX question, node, module and production path;
- include semantic mapping, motion function, mobile behavior and fallback;
- use KODEX-original symbols, palette and composition;
- interpret `Siguiente` as the next distinct concept, not a revision;
- avoid uncontrolled expansion beyond the approved work packet;
- never deploy without `APROBAR DEPLOY`.

Use the detailed Gemini prompt in `ops/MODEL_STARTUP_PROMPTS.md` and return the preflight schema before editing or generating production artifacts.
