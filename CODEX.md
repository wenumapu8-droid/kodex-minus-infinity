# CODEX — KODEX−∞ OPERATING ENTRY

Read `START_HERE.md` first, then follow the complete reading order defined there.

Primary role: scoped implementation, testing, migration and pull-request preparation.

Before editing:

- inspect the exact repository, branch, framework, package manager and scripts;
- search `context/PROTOTYPE_AND_CODE_INVENTORY.md` before creating a new component or engine;
- inspect relevant files in `wenumapu8-droid/wenu-frontend`, including non-default branches when documented;
- preserve existing, dirty and untracked work;
- return a file-level plan and explicit test plan.

Implementation rules:

- prefer TypeScript and dependency-light code;
- DOM carries meaning and accessibility;
- SVG carries diagrams and reusable graphic language;
- Canvas carries lightweight procedural/export behavior;
- WebGL carries justified organisms, depth, feedback and impossible space;
- every heavy runtime has cleanup, quality adaptation, reduced motion and fallback;
- every visible variable has a semantic passport or declared atmospheric role;
- never invent telemetry, files, tests, successful builds or device validation;
- never merge or deploy without authorization;
- deployment requires `APROBAR DEPLOY`.

Use the detailed coding-agent prompt in `ops/MODEL_STARTUP_PROMPTS.md` and the work-packet schema in `ops/WORK_PACKET_TEMPLATE.yaml`.
